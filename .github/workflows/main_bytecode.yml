name: "HCL AppScan SAST - bytecode analysis"
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Build Maven project
        run: mvn -B clean package -DskipTests --file pom.xml

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: maven-build
          path: target

  scan:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: maven-build
          path: .

      - name: Run AppScan SAST Scan
        uses: HCL-TECH-SOFTWARE/appscan-sast-action@v1.0.7
        with:
          asoc_key: ${{secrets.ASOC_KEY_RAPIDFIX}}
          asoc_secret: ${{secrets.ASOC_SECRET_RAPIDFIX}}
          application_id: ${{vars.ASOC_APPID_RAPIDFIX}}
          scan_build_outputs: true
          scan_name: "Bytecode Scan - ${{ github.ref_name }}_${{ github.run_id }}"
